# HW3 — Customized (Use your own model files)

This project is adapted to load user-provided model filenames: `spam_logreg_model.joblib` and `spam_tfidf_vectorizer.joblib`.

Replace the placeholder model files in /models with your actual model files if desired.